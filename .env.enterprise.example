# Azure OpenAI
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=
# OPENAI_API_VERSION=2024-10-21  # optional, required for non-v1 API

# NVIDIA (hosted at integrate.api.nvidia.com, or a self-hosted NIM gateway)
NVIDIA_API_KEY=
# A custom NIM endpoint is configured via the CLI provider URL / config backend_url,
# which overrides the default https://integrate.api.nvidia.com/v1
